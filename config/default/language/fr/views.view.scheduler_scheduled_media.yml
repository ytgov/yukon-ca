display:
  default:
    display_title: Master
    display_options:
      exposed_form:
        options:
          submit_button: Filtres
          reset_button_label: Réinitialiser
          exposed_sorts_label: 'Trier par'
      pager:
        options:
          expose:
            items_per_page_label: 'Éléments par page'
            items_per_page_options_all_label: '- Tout -'
            offset_label: Décalage
      fields:
        bundle:
          label: 'Type de média'
        status:
          label: Statut
          settings:
            format_custom_true: 'Publié le'
        operations:
          label: Operations
      filters:
        bundle:
          expose:
            label: Type
        status:
          expose:
            label: Statut
          group_info:
            group_items:
              1:
                title: 'Publié le'
        langcode:
          expose:
            label: Langue
  user_page:
    display_options:
      display_comment: 'Access to the user view is controlled via a custom RouteSubscriber. The high-level "view media" permission is added to satisfy the security_review module'
