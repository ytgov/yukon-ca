{#
/**
 * @file
 * Default theme implementation for a view template to display card collections.
 *
 * Available variables:
 * - attributes: HTML attributes for the container.
 * - rows: A list of rows for this list.
 *   - attributes: The row's HTML attributes.
 *   - content: The row's contents.
 * - title: The title of this group of rows. May be empty.
 * - list: Configuration for the UI Pattern.
 *   - columns: Number of columns in the list.
 *
 * @see template_preprocess_views_view_list()
 *
 * @ingroup themeable
 */
#}
{% if title %}
  <h3>{{ title }}</h3>
{% endif %}

{% set attributes = attributes|render|trim %}
{% if attributes -%}<div{{ attributes }}>{% endif %}

{{ pattern(list.pattern, {
  items: rows,
}, list.variant) }}

{% if attributes -%}</div>{% endif %}
